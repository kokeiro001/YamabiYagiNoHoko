#pragma once

#include "Selene.h"

using namespace Selene;

static const ColorF WHITE		(1.0f, 1.0f, 1.0f);
static const ColorF BLACK		(0.0f, 0.0f, 0.0f);
static const ColorF GRAY		(0.5f, 0.5f, 0.5f);

static const ColorF RED					(1.0f, 0.0f, 0.0f);
static const ColorF GREEN				(0.0f, 0.5f, 0.0f);
static const ColorF BLUE				(0.0f, 0.0f, 1.0f);
